<Page x:Class="SvwsIccImporter.View.AboutPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AboutPage">

    <StackPanel Margin="10">
        <ui:CardExpander ContentPadding="0" IsExpanded="True" Icon="{ui:ImageIcon 'pack://application:,,,/icon.png', Width=38, Height=38}" Margin="0,0,0,10">
            <ui:CardExpander.Header>
                <Grid Margin="0,0,10,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="{Binding ViewModel.ApplicationName}" />
                        <TextBlock Text="{Binding ViewModel.Copyright}" />
                    </StackPanel>
                    
                    <ui:TextBlock Grid.Column="1" VerticalAlignment="Center" Text="{Binding ViewModel.Version}" />
                </Grid>
            </ui:CardExpander.Header>

            <StackPanel>
                <ui:Anchor NavigateUri="{Binding ViewModel.ProjectUrl}"
                           HorizontalAlignment="Stretch"
                           HorizontalContentAlignment="Stretch"
                           Background="Transparent"
                           BorderThickness="0,1,0,1"
                           Padding="16"
                           CornerRadius="0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding ViewModel.ProjectUrl}" />
                        <ui:SymbolIcon Grid.Column="1" Symbol="Link24" />
                    </Grid>
                </ui:Anchor>
            </StackPanel>
        </ui:CardExpander>

        <ui:InfoBar IsOpen="True" IsClosable="False" Margin="0,0,0,10"
                    Message="Dieses Programm nutzt Bibliotheken von Drittanbietern. Diese sind im Folgenden aufgelistet. Besonderen Dank an die Entwickler dieser Bibliothken." />

        <ItemsControl ItemsSource="{Binding ViewModel.Libraries}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <ui:CardExpander DataContext="{Binding}" Margin="0,0,0,10">
                        <ui:CardExpander.Header>
                            <Grid Margin="0,0,10,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="{Binding PackageName}" />
                                    <ui:TextBlock Text="{Binding LicenseType}" Foreground="{ui:ThemeResource AccentTextFillColorPrimaryBrush}"></ui:TextBlock>
                                </StackPanel>
                                <ui:HyperlinkButton Grid.Column="1" NavigateUri="{Binding PackageUri}" Content="Projekt-Seite"></ui:HyperlinkButton>
                            </Grid>
                        </ui:CardExpander.Header>

                        <ui:TextBlock Text="{Binding Description}"></ui:TextBlock>
                    </ui:CardExpander>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>

    <!--<Grid>
    rid.Row="10">
            <ItemsControl ItemsSource="{Binding ViewModel.Libraries}" Margin="7">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <GroupBox DataContext="{Binding}" Margin="0,0,0,10">
                            <GroupBox.Header>
                                <ui:HyperlinkButton NavigateUri="{Binding PackageUrl}" FontSize="19">
                                    <TextBlock Text="{Binding PackageName}" />
                                </ui:HyperlinkButton>
                            </GroupBox.Header>

                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Text="{Binding Description}" TextWrapping="Wrap" />
                                <ui:HyperlinkButton NavigateUri="{Binding LicenseUrl}"
                                                            Grid.Row="1">
                                    <TextBlock Text="Lizenz anzeigen" />
                                </ui:HyperlinkButton>
                            </Grid>
                        </GroupBox>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>-->
</Page>
