<ui:FluentWindow x:Class="SvwsIccImporter.View.MainView"
                 x:Name="Window"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
                 xmlns:converter="clr-namespace:SvwsIccImporter.Converter"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:view="clr-namespace:SvwsIccImporter.View"
                 mc:Ignorable="d"
                 Title="SVWS ICC Importer" Height="500" Width="800">
    

    <DockPanel LastChildFill="True">
        <ui:TitleBar Title="SVWS ICC Importer"
                     DockPanel.Dock="Top">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/icon.png" />
            </ui:TitleBar.Icon>
            <ui:TitleBar.TrailingContent>
                <ui:TextBlock Margin="16" x:Name="TrailingContent" Text="{Binding SchoolName}" />
            </ui:TitleBar.TrailingContent>
        </ui:TitleBar>

        <ui:NavigationView x:Name="RootNavigation"
                           IsBackButtonVisible="Collapsed"
                           PaneDisplayMode="Top">
            <ui:NavigationView.MenuItems>
                <ui:NavigationViewItem Content="Importieren"
                                       Icon="{ui:SymbolIcon ArrowUpload24}"
                                       TargetPageType="{x:Type view:ImportPage}" />
            </ui:NavigationView.MenuItems>

            <ui:NavigationView.FooterMenuItems>
                <ui:NavigationViewItem Content="Einstellungen"
                                       Icon="{ui:SymbolIcon Settings24}"
                                       TargetPageType="{x:Type view:SettingsPage}" />
                <ui:NavigationViewItem Content="Über"
                                       Icon="{ui:SymbolIcon Info24}"
                                       TargetPageType="{x:Type view:AboutPage}" />
            </ui:NavigationView.FooterMenuItems>
        </ui:NavigationView>
    </DockPanel>
   
</ui:FluentWindow>
